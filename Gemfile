source "https://rubygems.org"

# GitHub Pages runs this exact dependency set in production.
gem "github-pages", group: :jekyll_plugins

# Required by Jekyll's local server on Ruby >= 3.0.
gem "webrick"
